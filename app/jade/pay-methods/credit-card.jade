.credit-card.loading(class=creatingFromScratch ? 'from-scratch' : '')
  .form.lexi-blue(id="", method="", action="")
    .card
      if creatingFromScratch
        .form-input
          label(for="card-number") Card Number
          .lex-holder#card-number
      .form-input.exp
        label(for="expiration-date") Exp. Date
        .lex-holder#expiration-date
      .form-input.cvv
        label(for="cvv") CVV
        .lex-holder#cvv
